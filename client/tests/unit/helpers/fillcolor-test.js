
import { fillcolor } from 'ararice/helpers/fillcolor';
import { module, test } from 'qunit';

module('Unit | Helper | fillcolor');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fillcolor([42]);
  assert.ok(result);
});

